silhouette {
  # Authenticator settings
  authenticator.authenticatorIdleTimeout=30 minutes
  authenticator.authenticatorExpiry=12 hours

  authenticator.cookie.signer.key = "[maximaseguridadparaiomarket2018bycollsteam]" // A unique encryption key

  authenticator.signer.key = "[maximaseguridadparaiomarket2018bycollsteam]" // A unique encryption key
  authenticator.crypter.key = "[maximaseguridadparaiomarket2018bycollsteam]" // A unique encryption key
}